# Reduction smoke artifacts

This folder receives PCA outputs for dense transformer embeddings and
TruncatedSVD outputs for sparse TF-IDF matrices. These are diagnostic smoke-test
artifacts only. Final PCA/SVD estimators must be fit on the training split.
