# Transformer smoke artifacts

Notebook `04_03_transformer_embeddings_and_reduction.ipynb` writes small,
reproducible BERT and FinBERT smoke-test arrays here. Generated `.npy`, mapping,
model metadata, and probability files are ignored by Git because they can be
recreated from the pinned recipe and source data.
